
pub mod auth;
pub mod user;
pub mod error;
pub mod models;
pub mod websocket;
pub mod dataapi;
pub mod transaction;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
